# SynapseTask
SynapseTask is a privacy-first, client-side task management application that bridges the gap between simple to-do lists and complex project management tools. Unlike competitors that charge high monthly subscriptions for AI features, SynapseTask operates on a (BYOK) , allowing users to pay only for the AI inference they actually use via OpenRouter.
